export enum EProductsEndpoints {
	CREATE = "/story/products/create",
	BASE = "/story/products",
	GETBYCAT = "/story/products/search",
}
export enum EAuthEndpoints {
	REGISTER = "/auth/register",
	LOGIN = "/auth/login",
	USER = "/auth/user",
	ADMIN = "/auth/admin",
}
export enum EUserEndpoints {
	BASE = "/User",
}
